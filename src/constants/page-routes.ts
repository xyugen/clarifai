export enum PageRoutes {
  HOME = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  FORGOT_PASSWORD = "/forgot-password",

  DASHBOARD = "/dashboard",
  SETTINGS = "/settings",
  UPLOAD = "/upload",
  STUDY = "/study",
  QUESTION = "/question",
  PROFILE = "/profile",
}
